{
  "name": "interceptor-landing",
  "version": "1.0.0",
  "description": "Interceptor Landing Page",
  "author": "Amit Bhavikatti",
  "license": "MIT",
  "main": "index.html",
  "scripts": {
    "start": "parcel src/public/*",
    "build": "parcel build src/public/* --public-url ./",
    "serve": "npm run build && NODE_ENV=development && nodemon server.js --config .nodemonrc"
  },
  "dependencies": {
    "bootstrap": "^4.1.2",
    "express": "^4.16.3",
    "helmet": "^3.13.0",
    "nodemon": "^1.18.3"
  },
  "devDependencies": {
    "parcel-bundler": "^1.6.1"
  }
}
